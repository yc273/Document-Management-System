# 智能文档管理系统 - 项目规划文档

## 📋 项目概述

### 项目名称
**智能文档管理系统 (Intelligent Document Management System)**

### 项目简介
一个基于Web的文档管理系统，支持文档上传、分类、检索、版本控制、权限管理等功能，适用于企业或个人的文档集中管理。

### 应用场景
- 企业文档集中管理
- 团队文件共享协作
- 个人文档云存储
- 项目资料归档

### 部署方式
- **前后端一体化部署**
- 支持局域网访问（手机、电脑、平板等设备）
- 单端口统一访问
- 本地存储 + SQLite数据库

---

## 🎯 核心功能模块

### 模块清单

| 序号 | 模块名称 | 功能点数量 | 优先级 |
|------|----------|------------|--------|
| 1 | 用户管理模块 | 8 | P0 |
| 2 | 文件夹管理模块 | 7 | P0 |
| 3 | 文档上传模块 | 8 | P0 |
| 4 | 文档管理模块 | 10 | P0 |
| 5 | 文档标签模块 | 6 | P1 |
| 6 | 文档检索模块 | 5 | P1 |
| 7 | 版本控制模块 | 5 | P1 |
| 8 | 文档共享模块 | 7 | P1 |
| 9 | 权限管理模块 | 5 | P2 |
| 10 | 在线预览模块 | 6 | P1 |
| 11 | 文档下载模块 | 5 | P0 |
| 12 | 回收站模块 | 5 | P1 |
| 13 | 数据统计模块 | 7 | P2 |
| 14 | 系统设置模块 | 5 | P2 |

**总计：92个功能点**

---

## 🛠️ 技术架构

### 技术栈选型

#### 后端技术
```yaml
开发语言: Python 3.8+
Web框架: Flask 2.0+
ORM框架: SQLAlchemy
用户认证: Flask-Login
RESTful: Flask-RESTful
跨域处理: Flask-CORS
数据库: SQLite 3.x（单文件、零配置）
缓存: Redis (可选)
文件存储: 本地文件系统
```

#### 前端技术
```yaml
基础: HTML5 + CSS3 + JavaScript ES6+
框架: Vue.js 3.x
UI组件: Element Plus
HTTP库: Axios
状态管理: Pinia (可选)
图表库: ECharts (数据统计)
```

#### 开发工具
```yaml
IDE: PyCharm / VS Code
数据库工具: DB Browser for SQLite / Navicat
API测试: Postman / Apifox
版本控制: Git
```

### 系统架构图

```
┌─────────────────────────────────────────────────────────┐
│                      客户端层                            │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────┐  │
│  │ PC浏览器 │  │ 手机浏览器│  │ 平板浏览器│  │ 其他设备│  │
│  └──────────┘  └──────────┘  └──────────┘  └────────┘  │
└─────────────────────────────────────────────────────────┘
                         ↕ HTTP/HTTPS
┌─────────────────────────────────────────────────────────┐
│                   Web服务层 (Flask)                      │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐│
│  │  静态文件服务 │  │  RESTful API │  │  用户认证中间件  ││
│  └─────────────┘  └─────────────┘  └─────────────────┘│
└─────────────────────────────────────────────────────────┘
                         ↕
┌─────────────────────────────────────────────────────────┐
│                    业务逻辑层                            │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────┐  │
│  │用户服务  │  │文档服务  │  │权限服务  │  │统计服务│  │
│  └──────────┘  └──────────┘  └──────────┘  └────────┘  │
└─────────────────────────────────────────────────────────┘
      ↕                ↕                ↕
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  SQLite数据库  │  │  本地文件存储  │  │  Redis缓存   │
│              │  │  /uploads/    │  │  (可选)      │
└──────────────┘  └──────────────┘  └──────────────┘
```

---

## 📁 项目目录结构

```
doc_management_system/
├── backend/                           # 后端项目
│   ├── app/                          # 应用主目录
│   │   ├── __init__.py              # 应用初始化
│   │   ├── config.py                # 配置文件
│   │   ├── extensions.py            # 扩展插件
│   │   │
│   │   ├── models/                   # 数据模型层
│   │   │   ├── __init__.py
│   │   │   ├── user.py              # 用户模型
│   │   │   ├── folder.py            # 文件夹模型
│   │   │   ├── file.py              # 文档模型
│   │   │   ├── tag.py               # 标签模型
│   │   │   ├── version.py           # 版本模型
│   │   │   ├── share.py             # 分享模型
│   │   │   ├── permission.py        # 权限模型
│   │   │   └── log.py               # 日志模型
│   │   │
│   │   ├── controllers/              # 控制器层
│   │   │   ├── __init__.py
│   │   │   ├── auth_controller.py   # 认证控制器
│   │   │   ├── folder_controller.py # 文件夹控制器
│   │   │   ├── file_controller.py   # 文档控制器
│   │   │   ├── tag_controller.py    # 标签控制器
│   │   │   ├── share_controller.py  # 分享控制器
│   │   │   └── stat_controller.py   # 统计控制器
│   │   │
│   │   ├── services/                 # 业务逻辑层
│   │   │   ├── __init__.py
│   │   │   ├── file_service.py      # 文档服务
│   │   │   ├── upload_service.py    # 上传服务
│   │   │   ├── search_service.py    # 搜索服务
│   │   │   └── share_service.py     # 分享服务
│   │   │
│   │   └── utils/                    # 工具类
│   │       ├── __init__.py
│   │       ├── file_util.py         # 文件工具
│   │       ├── auth_util.py         # 认证工具
│   │       ├── response.py          # 响应封装
│   │       └── decorators.py        # 装饰器
│   │
│   ├── uploads/                     # 文件上传目录
│   │   ├── documents/              # 文档文件
│   │   ├── avatars/                # 用户头像
│   │   └── temp/                   # 临时文件
│   │
│   ├── static/                      # 静态文件（前端打包后）
│   │   ├── assets/
│   │   ├── index.html
│   │   └── favicon.ico
│   │
│   ├── templates/                    # 模板文件（可选）
│   │
│   ├── requirements.txt              # Python依赖
│   ├── run.py                       # 启动文件
│   ├── db_init.py                   # 数据库初始化
│   └── README.md                    # 后端说明
│
├── frontend/                         # 前端项目（Vue）
│   ├── public/
│   │   └── favicon.ico
│   │
│   ├── src/
│   │   ├── assets/                  # 静态资源
│   │   │   ├── images/
│   │   │   └── styles/
│   │   │
│   │   ├── components/              # 公共组件
│   │   │   ├── FileList.vue        # 文档列表
│   │   │   ├── FileUpload.vue      # 文件上传
│   │   │   ├── FolderTree.vue      # 文件夹树
│   │   │   ├── SearchBar.vue       # 搜索栏
│   │   │   ├── TagManager.vue      # 标签管理
│   │   │   └── StatChart.vue       # 统计图表
│   │   │
│   │   ├── views/                   # 页面视图
│   │   │   ├── Login.vue           # 登录页
│   │   │   ├── Register.vue        # 注册页
│   │   │   ├── Layout.vue          # 布局页
│   │   │   ├── Dashboard.vue       # 首页
│   │   │   ├── FileManager.vue     # 文件管理
│   │   │   ├── Share.vue           # 分享页面
│   │   │   ├── Trash.vue           # 回收站
│   │   │   └── Statistics.vue      # 数据统计
│   │   │
│   │   ├── api/                     # API接口
│   │   │   ├── index.js            # API基础配置
│   │   │   ├── auth.js             # 认证API
│   │   │   ├── file.js             # 文档API
│   │   │   ├── folder.js           # 文件夹API
│   │   │   ├── tag.js              # 标签API
│   │   │   └── share.js            # 分享API
│   │   │
│   │   ├── router/                  # 路由配置
│   │   │   └── index.js
│   │   │
│   │   ├── store/                   # 状态管理
│   │   │   └── index.js
│   │   │
│   │   ├── utils/                   # 工具函数
│   │   │   ├── request.js          # 请求封装
│   │   │   └── common.js           # 通用工具
│   │   │
│   │   ├── App.vue                  # 根组件
│   │   └── main.js                  # 入口文件
│   │
│   ├── package.json                 # 项目配置
│   ├── vite.config.js              # Vite配置
│   └── README.md                    # 前端说明
│
├── docs/                             # 文档目录
│   ├── 项目规划文档.md              # 本文档
│   ├── 接口文档.md                  # API文档
│   ├── 数据库设计.md                # 数据库设计文档
│   └── 使用说明书.md                # 用户手册
│
├── scripts/                          # 脚本文件
│   ├── init_db.sql                  # 数据库初始化脚本
│   └── build.sh                     # 构建脚本
│
└── README.md                         # 项目主说明
```

---

## 🗄️ 数据库设计

### 数据表清单

| 序号 | 表名 | 中文名 | 主要字段 |
|------|------|--------|----------|
| 1 | sys_user | 用户表 | id, username, password, email, role |
| 2 | doc_folder | 文件夹表 | id, name, parent_id, user_id, path |
| 3 | doc_file | 文档表 | id, filename, file_type, file_size, folder_id |
| 4 | doc_tag | 标签表 | id, name, color, user_id |
| 5 | doc_file_tag | 文档标签关联表 | id, file_id, tag_id |
| 6 | doc_version | 版本表 | id, file_id, version_number, file_path |
| 7 | doc_share | 分享表 | id, share_code, file_id, expire_time |
| 8 | doc_permission | 权限表 | id, file_id, user_id, permission_type |
| 9 | sys_log | 操作日志表 | id, user_id, action, description |

### 数据库ER图

```
┌─────────────┐           ┌─────────────┐
│  sys_user   │           │  doc_folder │
│  (用户表)    │           │  (文件夹表)  │
├─────────────┤           ├─────────────┤
│ id          │───┐       │ id          │
│ username    │   │       │ name        │
│ password    │   │       │ parent_id   │◄─┐
│ email       │   │       │ user_id     │   │
│ role        │   └───────│ created_at  │   │
└─────────────┘           └─────────────┘   │
                                           │
        ┌─────────────┐                    │
        │  doc_file   │                    │
        │  (文档表)    │                    │
        ├─────────────┤                    │
        │ id          │                    │
        │ filename    │                    │
        │ file_type   │                    │
        │ folder_id   │────────────────────┘
        │ user_id     │
        │ file_path   │
        └─────────────┘
                │
        ┌───────┼───────┐
        ↓       ↓       ↓
┌──────────┐ ┌──────┐ ┌─────────┐
│doc_tag   │ │doc   │ │doc_share│
│(标签表)   │ │_file │ │(分享表) │
├──────────┤ │_tag  │ ├─────────┤
│ id       │ └──────┘ │ share_  │
│ name     │          │ code    │
└──────────┘          └─────────┘
```

### 详细表结构

#### 1. 用户表 (sys_user)
```sql
CREATE TABLE sys_user (
    id INT PRIMARY KEY AUTO_INCREMENT COMMENT '用户ID',
    username VARCHAR(50) NOT NULL UNIQUE COMMENT '用户名',
    password VARCHAR(128) NOT NULL COMMENT '密码（加密）',
    email VARCHAR(100) UNIQUE COMMENT '邮箱',
    nickname VARCHAR(50) COMMENT '昵称',
    avatar VARCHAR(255) COMMENT '头像路径',
    role VARCHAR(20) DEFAULT 'user' COMMENT '角色：admin/user',
    status TINYINT DEFAULT 1 COMMENT '状态：1启用 0禁用',
    storage_limit BIGINT DEFAULT 1073741824 COMMENT '存储限制（字节）',
    storage_used BIGINT DEFAULT 0 COMMENT '已使用存储（字节）',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_username (username),
    INDEX idx_email (email)
) COMMENT='用户表';
```

#### 2. 文件夹表 (doc_folder)
```sql
CREATE TABLE doc_folder (
    id INT PRIMARY KEY AUTO_INCREMENT COMMENT '文件夹ID',
    name VARCHAR(100) NOT NULL COMMENT '文件夹名称',
    parent_id INT DEFAULT 0 COMMENT '父文件夹ID（0为根目录）',
    user_id INT NOT NULL COMMENT '所属用户',
    path VARCHAR(500) COMMENT '完整路径',
    level INT DEFAULT 1 COMMENT '层级',
    sort_order INT DEFAULT 0 COMMENT '排序',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    FOREIGN KEY (user_id) REFERENCES sys_user(id),
    INDEX idx_user_parent (user_id, parent_id)
) COMMENT='文件夹表';
```

#### 3. 文档表 (doc_file)
```sql
CREATE TABLE doc_file (
    id INT PRIMARY KEY AUTO_INCREMENT COMMENT '文档ID',
    filename VARCHAR(255) NOT NULL COMMENT '文件名（含扩展名）',
    original_name VARCHAR(255) NOT NULL COMMENT '原始文件名',
    file_type VARCHAR(20) NOT NULL COMMENT '文件类型',
    file_size BIGINT NOT NULL COMMENT '文件大小（字节）',
    file_path VARCHAR(500) NOT NULL COMMENT '存储路径',
    folder_id INT DEFAULT 0 COMMENT '所属文件夹ID',
    user_id INT NOT NULL COMMENT '上传用户',
    file_hash VARCHAR(64) COMMENT '文件哈希值',
    version INT DEFAULT 1 COMMENT '当前版本号',
    download_count INT DEFAULT 0 COMMENT '下载次数',
    view_count INT DEFAULT 0 COMMENT '查看次数',
    is_deleted TINYINT DEFAULT 0 COMMENT '是否删除：1是 0否',
    deleted_at DATETIME COMMENT '删除时间',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    FOREIGN KEY (user_id) REFERENCES sys_user(id),
    FOREIGN KEY (folder_id) REFERENCES doc_folder(id),
    INDEX idx_user_folder (user_id, folder_id),
    INDEX idx_deleted (is_deleted)
) COMMENT='文档表';
```

#### 4. 标签表 (doc_tag)
```sql
CREATE TABLE doc_tag (
    id INT PRIMARY KEY AUTO_INCREMENT COMMENT '标签ID',
    name VARCHAR(50) NOT NULL COMMENT '标签名称',
    color VARCHAR(20) DEFAULT '#409EFF' COMMENT '标签颜色',
    user_id INT NOT NULL COMMENT '所属用户',
    use_count INT DEFAULT 0 COMMENT '使用次数',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    FOREIGN KEY (user_id) REFERENCES sys_user(id),
    UNIQUE KEY uk_user_name (user_id, name)
) COMMENT='标签表';
```

#### 5. 文档标签关联表 (doc_file_tag)
```sql
CREATE TABLE doc_file_tag (
    id INT PRIMARY KEY AUTO_INCREMENT COMMENT 'ID',
    file_id INT NOT NULL COMMENT '文档ID',
    tag_id INT NOT NULL COMMENT '标签ID',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '关联时间',
    FOREIGN KEY (file_id) REFERENCES doc_file(id) ON DELETE CASCADE,
    FOREIGN KEY (tag_id) REFERENCES doc_tag(id) ON DELETE CASCADE,
    UNIQUE KEY uk_file_tag (file_id, tag_id)
) COMMENT='文档标签关联表';
```

#### 6. 版本表 (doc_version)
```sql
CREATE TABLE doc_version (
    id INT PRIMARY KEY AUTO_INCREMENT COMMENT '版本ID',
    file_id INT NOT NULL COMMENT '文档ID',
    version_number INT NOT NULL COMMENT '版本号',
    file_path VARCHAR(500) NOT NULL COMMENT '文件路径',
    file_size BIGINT NOT NULL COMMENT '文件大小',
    remark VARCHAR(255) COMMENT '版本说明',
    created_by INT NOT NULL COMMENT '创建用户',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    FOREIGN KEY (file_id) REFERENCES doc_file(id) ON DELETE CASCADE,
    FOREIGN KEY (created_by) REFERENCES sys_user(id),
    INDEX idx_file (file_id)
) COMMENT='文档版本表';
```

#### 7. 分享表 (doc_share)
```sql
CREATE TABLE doc_share (
    id INT PRIMARY KEY AUTO_INCREMENT COMMENT '分享ID',
    share_code VARCHAR(32) NOT NULL UNIQUE COMMENT '分享码',
    file_id INT NOT NULL COMMENT '文档ID',
    share_password VARCHAR(50) COMMENT '分享密码',
    expire_days INT DEFAULT 0 COMMENT '有效天数（0为永久）',
    expire_time DATETIME COMMENT '过期时间',
    view_count INT DEFAULT 0 COMMENT '查看次数',
    download_count INT DEFAULT 0 COMMENT '下载次数',
    created_by INT NOT NULL COMMENT '创建用户',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    FOREIGN KEY (file_id) REFERENCES doc_file(id) ON DELETE CASCADE,
    FOREIGN KEY (created_by) REFERENCES sys_user(id),
    INDEX idx_code (share_code)
) COMMENT='文档分享表';
```

#### 8. 权限表 (doc_permission)
```sql
CREATE TABLE doc_permission (
    id INT PRIMARY KEY AUTO_INCREMENT COMMENT '权限ID',
    file_id INT NOT NULL COMMENT '文档ID',
    user_id INT COMMENT '用户ID（NULL表示公开）',
    permission_type VARCHAR(20) NOT NULL COMMENT '权限类型：read/write/admin',
    created_by INT NOT NULL COMMENT '授权用户',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    FOREIGN KEY (file_id) REFERENCES doc_file(id) ON DELETE CASCADE,
    FOREIGN KEY (user_id) REFERENCES sys_user(id),
    INDEX idx_file_user (file_id, user_id)
) COMMENT='文档权限表';
```

#### 9. 操作日志表 (sys_log)
```sql
CREATE TABLE sys_log (
    id INT PRIMARY KEY AUTO_INCREMENT COMMENT '日志ID',
    user_id INT NOT NULL COMMENT '用户ID',
    action VARCHAR(50) NOT NULL COMMENT '操作类型',
    module VARCHAR(50) NOT NULL COMMENT '模块名称',
    description VARCHAR(255) COMMENT '操作描述',
    ip_address VARCHAR(50) COMMENT 'IP地址',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    FOREIGN KEY (user_id) REFERENCES sys_user(id),
    INDEX idx_user (user_id),
    INDEX idx_created (created_at)
) COMMENT='操作日志表';
```

---

## 🔌 API接口设计

### 接口规范

#### 请求格式
```json
{
  "data": {},
  "timestamp": 1234567890
}
```

#### 响应格式
```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

#### 状态码说明
| 状态码 | 说明 |
|--------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未授权 |
| 403 | 禁止访问 |
| 404 | 资源不存在 |
| 500 | 服务器错误 |

### API接口清单

#### 1. 认证模块 (/api/auth)
```
POST   /api/auth/register       # 用户注册
POST   /api/auth/login          # 用户登录
POST   /api/auth/logout         # 退出登录
GET    /api/auth/info           # 获取当前用户信息
PUT    /api/auth/password       # 修改密码
PUT    /api/auth/profile        # 修改个人信息
```

#### 2. 文件夹模块 (/api/folder)
```
POST   /api/folder              # 创建文件夹
GET    /api/folder/list         # 获取文件夹树
GET    /api/folder/:id          # 获取文件夹详情
PUT    /api/folder/:id          # 修改文件夹
DELETE /api/folder/:id          # 删除文件夹
POST   /api/folder/move         # 移动文件夹
```

#### 3. 文档模块 (/api/file)
```
POST   /api/file/upload         # 上传文档
GET    /api/file/list           # 获取文档列表
GET    /api/file/:id            # 获取文档详情
PUT    /api/file/:id            # 修改文档
DELETE /api/file/:id            # 删除文档（移入回收站）
POST   /api/file/move           # 移动文档
POST   /api/file/copy           # 复制文档
GET    /api/file/download/:id   # 下载文档
GET    /api/file/preview/:id    # 预览文档
POST   /api/file/search         # 搜索文档
POST   /api/file/batch-delete   # 批量删除
```

#### 4. 标签模块 (/api/tag)
```
POST   /api/tag                 # 创建标签
GET    /api/tag/list            # 获取标签列表
PUT    /api/tag/:id             # 修改标签
DELETE /api/tag/:id             # 删除标签
POST   /api/file/:id/tag        # 为文档添加标签
DELETE /api/file/:id/tag/:tagId # 移除文档标签
GET    /api/file/:id/tags       # 获取文档标签
```

#### 5. 版本模块 (/api/version)
```
GET    /api/version/file/:id    # 获取文档版本列表
POST   /api/version             # 创建新版本
GET    /api/version/:id         # 获取版本详情
POST   /api/version/restore/:id # 恢复到指定版本
```

#### 6. 分享模块 (/api/share)
```
POST   /api/share/create        # 创建分享
GET    /api/share/list          # 获取我的分享列表
GET    /api/share/:code         # 访问分享（验证密码）
DELETE /api/share/:id           # 取消分享
GET    /api/share/access/:id    # 分享访问统计
```

#### 7. 回收站模块 (/api/trash)
```
GET    /api/trash/list          # 获取回收站列表
POST   /api/trash/restore/:id   # 恢复文档
DELETE /api/trash/delete/:id    # 永久删除
DELETE /api/trash/clear         # 清空回收站
```

#### 8. 统计模块 (/api/stat)
```
GET    /api/stat/storage        # 存储空间统计
GET    /api/stat/upload         # 上传统计
GET    /api/stat/file           # 文档数量统计
GET    /api/stat/log            # 操作日志
GET    /api/stat/dashboard      # 仪表板数据
```

#### 9. 系统模块 (/api/system)
```
GET    /api/system/info         # 系统信息
GET    /api/system/config       # 系统配置
PUT    /api/system/config       # 修改系统配置
```

---

## 🎨 前端页面设计

### 页面结构

```
登录/注册页
├── 登录页
└── 注册页

主应用布局
├── 侧边栏导航
│   ├── 文件管理
│   ├── 分享管理
│   ├── 回收站
│   └── 数据统计
│
└── 内容区域
    ├── 首页（仪表板）
    ├── 文件管理页
    │   ├── 文件夹树
    │   ├── 文件列表
    │   ├── 上传组件
    │   └── 搜索栏
    │
    ├── 分享管理页
    ├── 回收站页
    └── 数据统计页
```

### 页面功能清单

#### 1. 登录页
- 用户名/邮箱输入
- 密码输入
- 记住密码
- 登录按钮
- 跳转注册

#### 2. 注册页
- 用户名输入
- 邮箱输入
- 密码输入
- 确认密码
- 注册按钮
- 跳转登录

#### 3. 首页（仪表板）
- 存储空间使用率
- 文档数量统计
- 最近上传
- 快捷操作

#### 4. 文件管理页
- 文件夹树形导航
- 文档列表（网格/列表视图）
- 上传区域（拖拽）
- 搜索筛选
- 批量操作
- 右键菜单

#### 5. 分享管理页
- 我的分享列表
- 创建分享
- 分享设置
- 访问统计

#### 6. 回收站页
- 已删除文件列表
- 恢复操作
- 永久删除
- 清空回收站

#### 7. 数据统计页
- 存储空间图表
- 文档类型分布
- 上传趋势图
- 操作日志

---

## 🚀 部署方案

### 一体化部署架构

```
┌────────────────────────────────────┐
│         Flask 应用                 │
│                                    │
│  ┌──────────────────────────────┐ │
│  │  静态文件服务 (前端)          │ │
│  │  - index.html                 │ │
│  │  - assets/                    │ │
│  └──────────────────────────────┘ │
│                                    │
│  ┌──────────────────────────────┐ │
│  │  RESTful API 服务            │ │
│  │  - /api/*                    │ │
│  └──────────────────────────────┘ │
└────────────────────────────────────┘
         ↓            ↓
    SQLite数据库   文件存储目录
```

### 启动配置

#### run.py（启动文件）
```python
from app import create_app
from flask_cors import CORS

app = create_app()
CORS(app, resources={r"/api/*": {"origins": "*"}})

if __name__ == '__main__':
    # host='0.0.0.0' 允许局域网访问
    # port=5000 端口号
    app.run(host='0.0.0.0', port=5000, debug=True)
```

### 访问方式

#### 本地访问
```
http://localhost:5000
或
http://127.0.0.1:5000
```

#### 局域网访问
```
1. 查看本机IP地址（如：192.168.1.100）
2. 其他设备访问：
   http://192.168.1.100:5000
```

### 防火墙配置

#### Windows
- 首次运行会弹出防火墙提示
- 选择"允许访问"
- 选择"专用网络和公用网络"

#### Mac/Linux
```bash
# 如果端口被阻止
sudo ufw allow 5000
```

---

## 📝 开发步骤

### 第一阶段：项目初始化 ✅
- [x] 创建项目目录结构
- [x] 编写项目规划文档
- [ ] 配置开发环境
- [ ] 初始化数据库

### 第二阶段：后端开发
- [ ] 搭建Flask框架
- [ ] 创建数据模型
- [ ] 实现认证中间件
- [ ] 实现用户模块
- [ ] 实现文件夹模块
- [ ] 实现文档模块
- [ ] 实现标签模块
- [ ] 实现搜索模块
- [ ] 实现分享模块
- [ ] 实现统计模块

### 第三阶段：前端开发
- [ ] 搭建Vue项目
- [ ] 实现布局组件
- [ ] 实现登录注册页
- [ ] 实现文件管理页
- [ ] 实现上传组件
- [ ] 实现搜索组件
- [ ] 实现分享页
- [ ] 实现统计页

### 第四阶段：集成测试
- [ ] 前后端联调
- [ ] 功能测试
- [ ] 性能优化
- [ ] 局域网访问测试

### 第五阶段：软著准备
- [ ] 整理源代码
- [ ] 编写使用说明书
- [ ] 编写设计说明书
- [ ] 准备申请材料

---

## 📋 软著申请材料

### 1. 源代码文档
- 前端源代码（2000+行）
- 后端源代码（3000+行）
- 数据库脚本

### 2. 软件说明书
- 软件概述
- 功能说明
- 运行环境
- 安装部署
- 操作说明

### 3. 设计说明书
- 需求分析
- 总体设计
- 详细设计
- 接口设计
- 数据库设计

### 4. 其他材料
- 软件著作权申请表
- 申请人身份证明
- 软件截图

---

## 🎯 项目目标

### 功能目标
- ✅ 支持14个核心模块
- ✅ 实现92个功能点
- ✅ 支持局域网多设备访问
- ✅ 支持文件上传、管理、共享

### 技术目标
- ✅ 前后端一体化部署
- ✅ RESTful API设计
- ✅ 响应式UI（适配手机）
- ✅ 完整的权限控制

### 软著目标
- ✅ 源代码3000行以上
- ✅ 功能模块清晰完整
- ✅ 文档齐全规范

---

## 📞 联系方式

如有问题，请参考项目文档或提交Issue。

---

**文档版本：** v1.0
**最后更新：** 2025-01-XX
**文档状态：** 正在完善中
